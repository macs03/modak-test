import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  separator: {
    // marginVertical: 30,
    height: 1,
    width: "95%",
    backgroundColor: "#aaa",
    alignSelf: "center"
  }
});

export default styles;
